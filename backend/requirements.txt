# ─────────────────────────────────────────────────────────────────────────────
# Nexora Backend — Production Requirements
# Compatible with Python 3.11+ (tested locally on Python 3.14 / Docker 3.11)
# Minimal and curated — NOT a raw pip freeze.
# All transitive deps pinned to prevent resolver backtracking in Docker builds.
# ─────────────────────────────────────────────────────────────────────────────

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
starlette==0.41.3
python-multipart==0.0.12
h11==0.14.0
websockets==13.1
httptools==0.6.4

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6
greenlet==3.1.1
mako==1.3.9

# ── Auth / Security ───────────────────────────────────────────────────────────
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
cryptography==43.0.3
cffi==1.17.1
pycparser==2.22
ecdsa==0.19.1
pyasn1==0.6.1
rsa==4.9.1

# ── Settings / Config ─────────────────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1
pydantic-core==2.27.1
python-dotenv==1.0.1
annotated-types==0.7.0
typing_extensions==4.12.2
typing-inspection==0.4.0

# ── HTTP / Async ──────────────────────────────────────────────────────────────
httpx==0.27.2
httpcore==1.0.7
anyio==4.7.0
sniffio==1.3.1
certifi==2024.8.30
idna==3.10
charset-normalizer==3.4.0
urllib3==2.2.3

# ── OpenAI ────────────────────────────────────────────────────────────────────
openai==1.57.0
jiter==0.8.0
distro==1.9.0

# ── Document Processing (PDF/text) ────────────────────────────────────────────
pymupdf==1.24.14

# ── RAG / Text Splitting ──────────────────────────────────────────────────────
# Pin transitive deps explicitly to prevent pip backtracking on Docker
langchain-text-splitters==0.3.2
langchain-core==0.3.29
langsmith==0.1.147
orjson==3.10.12
requests==2.32.3
packaging==24.2

# ── Multipart / File uploads ──────────────────────────────────────────────────
python-dateutil==2.9.0.post0
six==1.17.0

# ── Pytest (dev/test only — safe to include, not used in production) ───────────
pytest==8.3.4
pytest-asyncio==0.24.0
anyio[trio]==4.7.0
